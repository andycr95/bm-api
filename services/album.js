/* const MongoLib = require('../lib/mongo'); */

class AlbumService { 

/*     constructor() {
        this.collection = 'albums'
        this.mongodb = new MongoLib();
    }

    async getAlbums() {
        const albums = await this.mongodb.getAll(this.collection);
        return albums || [];
    }

    async getAlbum({ id }) {
        const album = await this.mongodb.get(this.collection, id);
        return album || {};
    }

    async CreateAlbums({ album }) {
        const createdAlbum = await this.mongodb.create(this.collection, album);
        return createdAlbum
    }

    async UpdateAlbum({ id, album } = {}) {
        const updatedAlbum = await this.mongodb.update(this.collection, id, album);
        return updatedAlbum
    }

    async DeleteteAlbum({ id }) {
        const deletedAlbum = await this.mongodb.delete(this.collection, id);
        return deletedAlbum;
    } */
 }

 module.exports = AlbumService;