/* const MongoLib = require('../lib/mongo'); */

class ArtistService { 

/*     constructor() {
        this.collection = 'artists'
        this.mongodb = new MongoLib();
    }

    async getArtists() {
        const artists = await this.mongodb.getAll(this.collection);
        return artists || [];
    }

    async getArtist({ id }) {
        const artist = await this.mongodb.get(this.collection, id);
        return artist || {};
    }

    async CreateArtist({ artist }) {
        const createdArtist = await this.mongodb.create(this.collection, artist);
        return createdArtist
    }

    async UpdateArtist({ id, artist } = {}) {
        const updatedArtist = await this.mongodb.update(this.collection, id, artist);
        return updatedArtist
    }

    async DeleteteArtist({ id }) {
        const deletedArtist = await this.mongodb.delete(this.collection, id);
        return deletedArtist;
    } */
 }

 module.exports = ArtistService;